/*
Las variables que se ocupan dentro de javascript son 3:
var: que actualmente está en sustitución
let: es una variable "protegida" porque solo funciona dentro de una funcion o método declarado en un fragmento de codigo
const: la cual es el valor constante en todo el documento y no puede cambiar su estado
*/

let x = "x";
if(true) {
    //declaramos una constante
    const c = y;
    console.log(x);
}